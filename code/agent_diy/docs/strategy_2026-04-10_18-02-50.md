# DIY 版本策略文档（2026-04-10 18:02:50）

本文档总结当前仓库中 `2026-04-10 18:02:50` 这一版 `agent_diy` 的代码思路、算法流程、模块职责与策略特点。该版本对应的总体定位是：

- 运行形态上是“教师-学生混合框架”。
- 实际推理上因为 `TEACHER_ONLY = True`，当前提交/评测时由教师规划器直接决策。
- 代码中仍然保留了学生模型、PPO 更新、模仿学习接口，用于后续切回训练型版本。

核心入口文件：

- `agent_diy/agent.py`
- `agent_diy/feature/preprocessor.py`
- `agent_diy/algorithm/algorithm.py`
- `agent_diy/model/model.py`
- `agent_diy/workflow/train_workflow.py`
- `agent_diy/conf/conf.py`

## 1. 总体架构

这版可以理解成五层结构：

1. `workflow`
   - 驱动环境 reset、step、episode 结束、样本收集、监控上报、定时存模型。
2. `agent`
   - 负责把环境观测交给预处理器。
   - 负责把教师动作、教师概率、学生模型输出整合成最终动作。
   - 当前因为 `TEACHER_ONLY = True`，最终动作直接走教师。
3. `preprocessor`
   - 整个版本最核心的“教师策略本体”。
   - 负责全局地图记忆、NPC 风险建模、路径规划、目标选择、动作打分。
4. `model`
   - 一个轻量 MLP actor-critic。
   - 当前纯教师模式下不参与真实决策，但仍保留保存/加载接口。
5. `algorithm`
   - 提供 PPO + imitation 的训练逻辑。
   - 当前纯教师模式下 `Agent.learn()` 直接返回零损失，不真的训练。

一句话概括：

`这版真正决定分数的，是 preprocessor 里的规则规划器，而不是神经网络。`

## 2. 配置层设计

配置集中在 `agent_diy/conf/conf.py`。

### 2.1 运行模式

- `TEACHER_ONLY = True`
  - 含义：推理时直接采用教师动作，不走学生策略。
  - 影响：训练侧的模型、损失、训练步数依然保留接口，但真实策略表现由教师规划器决定。

### 2.2 地图与特征

- 地图全局大小：`128 x 128`
- 观测窗口：`21 x 21`
- 构建给模型的局部特征窗口：`7 x 7`
- 总特征维度：`LOCAL_FEATURE_DIM + GLOBAL_FEATURE_DIM = 245 + 26 = 271`

### 2.3 规划相关参数

主要包括：

- `REPLAN_INTERVAL = 2`
- `LOW_BATTERY_RATIO = 0.60`
- `RETURN_CHARGE_BUFFER = 34`
- `TARGET_CHARGE_BUFFER = 44`
- `DIRTY_CLUSTER_RADIUS = 3`
- `FRONTIER_CLUSTER_RADIUS = 4`
- `PATH_FOLLOW_BONUS = 5.2`

这意味着：

- 基本每 2 步就允许重规划一次。
- 电量控制比较保守。
- 脏污和 frontier 目标都先做稀疏聚类，再从候选里选目标。

### 2.4 贪吃蛇/边界相关参数

这版已经引入了两套重要偏置：

- `SNAKE_*`
  - 强化横平竖直、按行推进、减少回头。
- `BOUNDARY_*`
  - 强化沿“软边界”滑行，减少穿越已经反复清扫的空地。

### 2.5 NPC 风险参数

这版已经引入 NPC 电磁场模型：

- `NPC_COLLISION_RADIUS`
- `NPC_PREDICT_RADIUS`
- `NPC_CENTER_HARD_RADIUS`
- `NPC_CENTER_SOFT_RADIUS`
- `NPC_FIELD_CENTER_SCALE`
- `NPC_FIELD_NPC_SCALE`
- `NPC_FIELD_PRED_SCALE`
- `NPC_FIELD_MID_SCALE`

这使得 NPC 风险不再只是简单圆形阈值，而是“硬禁区 + 连续衰减风险场”。

### 2.6 训练相关参数

虽然当前是纯教师模式，但代码仍保留：

- PPO clip
- entropy 系数
- value loss 系数
- imitation loss 衰减
- teacher mix 衰减

这说明该版本的设计目标不是纯规则器，而是为后续“教师带学生”预留完整链路。

## 3. Agent 层工作流

`agent_diy/agent.py` 负责把框架接口和策略逻辑串起来。

### 3.1 初始化

Agent 初始化时会创建：

- `Model`
- `Adam optimizer`
- `Algorithm`
- `Preprocessor`

即使当前 `TEACHER_ONLY = True`，这些对象仍会构造出来，以便兼容框架的保存、加载、训练接口。

### 3.2 `observation_process`

流程：

1. 规范化环境输入结构。
2. 调用 `preprocessor.feature_process(...)`。
3. 得到：
   - 特征向量
   - 合法动作 mask
   - reward
4. 额外塞入教师信息：
   - `teacher_action`
   - `teacher_prob`
   - `teacher_force`
   - `teacher_mix_bias`
   - `teacher_weight`
   - `policy_weight`

所以从接口设计上，这版 `ObsData` 已经是“规则策略 + 学习策略共存”的格式。

### 3.3 `predict`

当前最关键的逻辑是：

- 如果 `TEACHER_ONLY = True`
  - 直接走 `_teacher_fallback_act_data(...)`
  - 不做模型前向，不做 teacher-student 混合

如果未来关闭 `TEACHER_ONLY`，则会变成：

1. 学生模型前向得到 logits 和 value
2. 对非法动作做 masked softmax
3. 按训练步数和局面危险度计算推理期 `teacher_mix`
4. 把学生概率与教师概率混合
5. 采样或贪心输出动作

所以现在虽然代码结构里叫“hybrid”，但真实执行路径是纯教师。

### 3.4 `learn`

当前逻辑：

- `TEACHER_ONLY = True` 时，直接返回一组 0 loss
- 不进入 `Algorithm.learn()`

所以这版训练时：

- 可能仍然存在 episode rollout
- 可能仍然保存 `ckpt-0.pkl`
- 但不会真正更新网络参数

### 3.5 `save_model / load_model`

这版仍保存神经网络权重与 metadata。

metadata 里最关键的是：

- `checkpoint_tag`
- `timestamp`
- `feature_dim`
- `train_step`

加载时会检查 `checkpoint_tag`，防止不同代码代之间的模型错载。

## 4. Preprocessor：教师策略核心

`agent_diy/feature/preprocessor.py` 是整套策略的主体。

它同时承担了：

- 世界模型维护
- 动态风险感知
- 路径规划
- 目标选择
- 动作打分
- 特征构造
- reward 构造

## 5. 内部状态与世界模型

### 5.1 地图编码

全局地图 `map_state` 使用：

- `UNKNOWN = -1`
- `OBSTACLE = 0`
- `CLEAN = 1`
- `DIRTY = 2`

这是一张 `128 x 128` 的全局记忆地图。

### 5.2 历史统计

预处理器还维护：

- `visit_count`
  - 每个格子被走过多少次
- `clean_pass_count`
  - 在已清洁地面上重复通过的次数
- `last_visit_step`
  - 每个格子最近一次访问的步数

这些量是路径代价函数的核心输入，用于抑制重复路径。

### 5.3 动态实体

维护的信息包括：

- 机器人当前位置 `cur_pos`
- 上一步位置 `last_pos`
- 充电桩列表 `chargers`
- NPC 当前位 `npc_positions`
- NPC 上一帧位置 `npc_prev_positions`
- NPC 一步预测位 `npc_pred_positions`
- NPC 出生/活动中心 `npc_centers`

### 5.4 规划状态

还会维护：

- `charge_mode`
- `goal`
- `goal_kind`
- `path`
- `last_plan_step`
- `pending_action`
- `pending_prob`

所以这不是“每步纯反应式”策略，而是一个带内部规划状态的在线规划器。

## 6. 观测解析流程

`feature_process()` 的主流程是：

1. `_parse_env_obs`
2. `_build_legal_action`
3. `_select_action`
4. `_build_feature`
5. `reward_process`

### 6.1 `_parse_env_obs`

这一步会把环境原始结构标准化，并更新内存状态。

具体包括：

- 读取 `step_no / battery / score / dirt_cleaned / total_dirt / charge_count`
- 更新当前位置
- 记录上一帧状态
- 解析 `legal_action`
- 读取 `21 x 21 map_info`
- 调用 `_integrate_local_view()` 把局部视野写回全局地图
- 调用 `_update_stuck_state()` 检测是否撞墙/卡住
- 读取 `step_cleaned_cells`
- 更新充电桩
- 更新 NPC
- 更新当前位置的访问计数与重复通过计数

### 6.2 `_integrate_local_view`

这是全局地图记忆的基础。

做法是：

- 以机器人当前位置为中心
- 将局部 `21 x 21` 的每个观测格写回全局 `map_state`

这样随着移动，机器人会逐渐拼出完整地图。

### 6.3 `_update_stuck_state`

这一步非常重要。

触发条件：

- 当前坐标和上一步完全一样
- 且上一步有动作

说明：

- 机器人尝试移动，但没有真的动起来
- 一般意味着撞墙或无效移动

处理：

- `stuck_chain += 1`
- 清空 `path`
- 清空 `goal`
- 根据上一步动作方向，把前方格子记成障碍
- 如果是斜向撞墙，还会把侧边格子也推断成障碍

这让机器人在后续观测时能主动绕开这些无效方向。

## 7. NPC 风险模型

这版 NPC 风险分成两层：

### 7.1 动态硬碰撞屏蔽

`_would_hit_npc()` 和 `_npc_dynamic_hard_block()` 负责短期硬屏蔽。

逻辑包括：

- 靠近 NPC 当前位
- 靠近 NPC 预测位
- 靠近 NPC 当前位和预测位之间的中点
- 靠近这些点再加一圈冗余缓冲

这用于直接排除危险动作和危险路径点。

### 7.2 电磁场软惩罚

`_npc_zone_penalty()` 使用势场模型：

- 对 NPC 活动中心加场
- 对 NPC 当前位加场
- 对 NPC 预测位加场
- 对当前位置与预测位置的中点加场

公式是：

`strength / (distance^2 + soften)`

含义：

- 越近风险越高
- 距离远时快速衰减

这一步不是硬禁止，而是进入路径代价和动作打分。

### 7.3 NPC 中心硬禁区

`_npc_zone_hard_block()` 会把 NPC 出生/活动中心附近一定半径直接视为不可走。

所以整体上是：

- 中心区硬禁
- 动态轨迹周围硬禁
- 更大范围内软惩罚

## 8. 合法动作过滤

`_build_legal_action()` 会做三层过滤：

1. 环境原始合法动作
2. 局部地图是否撞墙
3. 全局地图是否可通行
4. 是否落入 NPC 动态硬风险

如果全部过滤没了，还会走两层退化逻辑：

- `_build_recovery_mask`
- `_build_best_effort_mask`

这保证不会因为 mask 太严导致完全无动作可走。

### 8.1 局部墙体判断

`_is_local_move_passable()`

规则：

- 正常方向只要目标格不是障碍
- 斜向移动要求两个侧边都可通过

这避免了斜向穿墙。

### 8.2 全局通行判断

`_is_global_move_passable()`

规则：

- 目标格必须不是全局已知障碍
- 如果是斜向，两个侧边也必须是全局可通行
- `UNKNOWN` 是否可走由 `allow_unknown` 控制

## 9. 总体决策流程

真正选动作在 `_select_action()`。

流程如下：

1. `_trim_path_head()`
   - 如果路径头已经是当前位置，先弹掉
2. 判断是否进入回充模式
3. 判断是否需要重规划
4. 如果要重规划，调用 `_replan()`
5. 对 8 个动作做局部打分 `_score_actions()`
6. 如果已有全局路径，就给路径第一步再加一个 `PATH_FOLLOW_BONUS`
7. 用 `_scores_to_probs()` 把动作分转成概率
8. 纯教师模式下取 argmax 对应动作

所以最终动作不是纯路径跟随，而是：

`全局路径建议 + 局部动作打分修正`

## 10. 回充逻辑

### 10.1 进入回充模式

`_need_charge()` 的条件：

- 当前电量 <= 到最近充电桩的真实距离 + `RETURN_CHARGE_BUFFER`
- 或者电量比例 <= `LOW_BATTERY_RATIO`

这里不是简单直线距离，而是：

- 如果已有 `charger_dist_map` 就用真实可达距离
- 否则退化到启发式距离

### 10.2 退出回充模式

在 `_select_action()` 里：

- 若已经站在充电桩上
- 且电量 >= `98%`
- 就退出回充模式

### 10.3 充电桩目标选择

`_pick_best_charger_goal()` 会枚举所有充电桩 `3x3` 范围内的可站立格。

打分项包括：

- 距离
- 访问次数
- clean pass 次数
- 充电入口局部重复密度

所以不是单纯选最近桩，而是选“更容易进、重复更少”的桩入口。

## 11. 何时重规划

`_need_replan()` 返回 True 的情况包括：

- 当前没有路径
- 距离上次规划太久
- 当前卡住了
- 当前没有目标
- dirty 目标已经不脏了
- 充电目标已经到达
- 当前路径前 6 步里存在 NPC 风险

所以这个规划器不是每步都全图重算，而是“按需频繁重规划”。

## 12. 全局规划流程

`_replan()` 的步骤：

1. 用 `_build_bfs_tree(allow_unknown=True)` 从当前位置做一张全图 BFS 树
2. 用 `_build_multi_source_dist(...)` 对所有充电桩做多源 BFS 距离图
3. 根据当前模式决定选哪类目标：
   - `charge_mode` 下优先找充电桩
   - 否则先找 dirty
   - 再找 frontier
   - 再在低电时兜底找充电桩
4. 调用 `_plan_weighted_path(goal, allow_unknown=True)` 做加权 A*
5. 如果 A* 失败，再用 BFS 前驱树回溯一条普通路径

## 13. 路径搜索

### 13.1 BFS 树

`_build_bfs_tree()`

作用：

- 给 dirty/frontier 候选打距离
- 作为 A* 失败时的兜底路径

特点：

- 支持 8 邻接
- 斜向仍做侧边可通行检查
- 会避开 NPC 硬禁区

### 13.2 多源 BFS

`_build_multi_source_dist()`

作用：

- 构建充电桩距离图
- 供回充决策和目标可返航性判断使用

### 13.3 加权路径搜索

`_plan_weighted_path()`

本质是 A*：

- `g = 当前累计路径代价`
- `h = 到目标的切比雪夫距离`
- `f = g + h`

这版还没有后面的加权启发式优化，仍是标准 `f = g + h`。

## 14. 路径代价函数

`_transition_cost(cur_pos, next_pos)` 是整个教师规划器的“灵魂函数”。

组成如下：

### 14.1 正向代价

- 基础移动代价
- 斜走额外代价
- 最近刚经过的惩罚
- `visit_count` 重复访问惩罚
- `clean_pass_count` 已清洁路面重复通过惩罚
- `npc_zone_penalty` 风险惩罚
- `interior_clean_penalty`：深入大块纯 clean 区的惩罚
- `snake_penalty`：违背蛇形推进方向的惩罚

### 14.2 负向奖励

- `dirty_bonus`
- `frontier_bonus`
- `boundary_bonus`

其中：

- `dirty_bonus` 奖励踩脏地
- `frontier_bonus` 奖励贴近未知边界
- `boundary_bonus` 奖励沿软边界滑动

### 14.3 回充阶段的动态缩放

如果当前是回充模式：

- `dirty_bonus` 和 `frontier_bonus` 会按电量裕量缩小
- `snake_scale` 也会缩小

含义是：

- 电量紧时更保守，优先安全回桩
- 电量宽松时允许顺路拿一点收益

## 15. 贪吃蛇逻辑

这一版的“贪吃蛇”不是区域锁定那种强结构，而是全局序偏置。

### 15.1 序号定义

`_serpentine_index()`

定义方式：

- 偶数行：从左到右
- 奇数行：从右到左

这就是一个标准的横向牛耕式顺序。

### 15.2 偏置函数

`_serpentine_bias(cur_pos, next_pos)`

输出正负值表示：

- 顺着蛇形方向走：正奖励
- 反着走：负奖励
- 到行尾时上下换道：允许一定正偏置
- 在前方还有活时提前换道：给负偏置

也就是说它不是刚性的“只能蛇形”，而是把蛇形作为一个强偏好注入代价函数和动作打分。

## 16. 边界贴边逻辑

### 16.1 软墙定义

`_is_soft_wall(pos)` 认为以下都属于“软墙”：

- 真障碍
- NPC 风险过高格
- 高重复 clean 格

### 16.2 边界奖励

`_boundary_bonus(pos)` 会统计：

- 周围几个方向是软墙
- 周围几个方向还是高价值开放区

若一个位置恰好位于“墙边/边缘/软边界”附近，且旁边还有可探索价值，则给正奖励。

它的作用是：

- 减少在开阔 clean 区中间瞎穿
- 鼓励沿边推进
- 提高覆盖效率

## 17. 目标选择

### 17.1 脏污目标

`_pick_best_dirty_goal()`

流程：

1. 扫描全图所有 dirty 格
2. 用 `_cluster_cells()` 做稀疏候选聚类
3. 对候选点评分

评分项包括：

- 局部 dirty 密度
- frontier 收益
- 当前到该点的距离
- 访问次数
- clean pass 次数
- NPC 风险
- 若当前目标没变，给一点目标保持奖励

### 17.2 Frontier 目标

`_pick_best_frontier_goal()`

与 dirty 类似，只是更偏向：

- 未知区域增益
- 新探索价值

### 17.3 候选聚类

`_cluster_cells()`

本质不是复杂聚类，而是：

- 先按权重排序
- 再做“彼此至少间隔一定半径”的稀疏采样

它的目的是降低候选数量、减少规划开销。

## 18. 动作打分

即使有全局路径，最终一步仍由 `_score_actions()` 决定。

每个动作的得分来自：

- `-1.6 * transition_cost`
- 直接踩到脏地的奖励
- frontier 增益
- 访问/重复通过惩罚
- NPC 风险惩罚
- 蛇形偏置
- 边界贴边奖励
- 如果走回头路，额外减分
- 如果刚卡住，还继续重复同动作，重罚

此外：

- 回充模式下，靠近充电桩加分
- 踩进充电桩直接大奖励
- 非回充模式下，靠近当前目标加分

最后如果某动作正好等于路径第一步，再加 `PATH_FOLLOW_BONUS`。

所以这版是：

`路径指导方向，动作评分决定最终一步。`

## 19. 特征构造

虽然纯教师下模型不参与决策，这版仍会构建完整特征。

### 19.1 局部特征

包含 `7 x 7` 五通道：

- `local_map`
- `local_visit`
- `local_transit`
- `local_risk`
- `local_frontier`

### 19.2 全局标量特征

共 26 维，主要包括：

- 步数归一化
- 电量归一化
- 清扫进度
- 当前位置归一化
- 最近充电桩距离
- 最近 NPC 距离
- 是否回充模式
- 是否站在充电桩上
- 当前路径长度
- 周边未知密度
- 当前视野脏污数量
- 全图脏污数量
- 目标方向
- 当前位置 visit/transit
- charge slack
- stuck_chain
- explored ratio
- 当前 frontier gain
- charge_count
- 当前目标类型 one-hot

这些特征主要是为了保留学生模型训练能力。

## 20. 奖励函数

`reward_process()` 主要用于训练侧，不直接决定教师动作。

组成：

- 每步基础惩罚
- 本步清扫格子奖励
- 卡住惩罚
- 新进入充电桩奖励
- 当前处于高 NPC 风险区惩罚
- 在 clean 地面重复通过惩罚
- 邻近未知区域奖励

在 workflow 中，终局还会加额外 bonus：

- `truncated` 跑满步数时给正 bonus
- `terminated` 提前失败时给负 bonus

## 21. Workflow 运行流程

`train_workflow.py` 的流程是：

1. `env.reset`
2. `agent.reset`
3. `agent.load_model(id='latest')`
4. 获取初始观测并预测第一步动作
5. 进入 episode loop：
   - `env.step(action)`
   - 更新当前环境统计
   - 调 `observation_process`
   - 若未结束则继续预测下一步
   - 组装 `SampleData`
6. 达到 chunk 大小时送样本
7. 结束时做：
   - final bonus
   - 游戏日志
   - 监控上报
   - 窗口指标汇总

### 21.1 纯教师模式下的特殊点

虽然 workflow 仍然会收集 `SampleData`，但：

- `workflow()` 外层只有 `not Config.TEACHER_ONLY` 时才 `send_sample_data`
- `Agent.learn()` 在纯教师模式下也不会真的训练

所以这版本质上是：

- 环境持续 rollout
- 监控持续更新
- 模型文件按平台要求保存
- 但不发生真正的参数学习

## 22. 学生模型与训练器

### 22.1 模型结构

`model.py` 定义的是一个 3 层 MLP：

- `271 -> 256 -> 192 -> 128`
- 激活：`GELU`
- 带 `LayerNorm`
- actor head 输出 8 个动作 logits
- critic head 输出 1 个 value

### 22.2 Algorithm

`algorithm.py` 是 PPO + imitation 混合损失：

- `value_loss`
- `policy_loss`
- `entropy_loss`
- `imitation_loss`

总损失：

`vf_coef * value_loss + policy_loss - beta * entropy_loss + imitation_coef * imitation_loss`

其中：

- `teacher_weight` 控制模仿损失在哪些样本上更强
- `policy_weight` 控制 PPO 损失在哪些样本上更强

但因为当前 `TEACHER_ONLY = True`，这部分只是接口预留。

## 23. 这一版的策略本质

用一句工程化的话总结：

`这版是一个带全局地图记忆的纯教师规划器，使用 BFS/A* 做目标到达，使用蛇形偏置和边界偏置来提高覆盖效率，使用电磁场 NPC 风险函数和硬碰撞屏蔽保证安全，使用充电桩真实距离图做保守回充。`

比它前面的朴素版本强的点主要是：

- 不再只是追最近脏点
- 开始抑制重复 clean 路径
- 开始有系统性的横向蛇形偏置
- 开始把 NPC 看成连续风险场而不是死半径
- 回充不只是最近桩，而是入口代价更低的桩格

## 24. 这一版的主要优点

- 纯教师模式下比较稳定。
- 已经具备全局地图记忆。
- 已经具备可解释的回充、避险、扫图偏好。
- 规则与模型接口分离，后续容易继续演化。
- 训练/监控/导出链路完整。

## 25. 这一版的主要局限

与后面你让我继续优化过的版本相比，这一版的主要局限是：

- 没有区域锁定清扫。
- 没有真正的房间/小角落收尾机制。
- 贪吃蛇偏置还是全局弱偏置，不是强结构覆盖。
- frontier 和 dirty 的选择仍偏“候选点打分”，不是“区域级调度”。
- 电磁场与贴边已经存在，但还没有做高性能缓存和局部化优化。
- 学生模型虽然接口完整，但因为 `TEACHER_ONLY = True`，实际没有形成可学习可超越的在线模型。

## 26. 适合如何使用这版

如果把这版作为继续研发的基线，最适合的定位是：

- 作为“带蛇形和 NPC 电磁场的纯教师基线”
- 后续继续在此基础上加：
  - 区域锁定
  - 小房间清扫收尾
  - 门口断区
  - 回充低重复路径优化
  - 学生模型蒸馏与 PPO 微调

如果只是做比赛提交，它的特点是：

- 比更早的普通贪心强
- 比后期那些重型覆盖逻辑更稳、更简单
- 但地图利用率和尾部补扫能力还不是最强

## 27. 总结

`2026-04-10 18:02:50` 这一版已经不是简单 baseline，而是一套中等复杂度的教师规划器。

它的核心思想可以概括为：

- 用全局记忆恢复地图
- 用 BFS/A* 做真实路径规划
- 用脏污聚类和 frontier 聚类做目标候选筛选
- 用重复惩罚减少无效路径
- 用蛇形偏置提高横向推进的规律性
- 用边界奖励减少在大空地里穿来穿去
- 用 NPC 电磁场函数和硬禁区保证安全
- 用真实充电距离和入口代价控制回充

如果后续你要继续升级它，最自然的方向就是：

- 把“蛇形偏置”升级成“区域覆盖策略”
- 把“候选点打分”升级成“区域级调度”
- 再把这套教师规则蒸馏给学生模型

