# Learnings

2026-04-25 - T0: `origin/linux-LTSPPO-charge-constraint` benchmark design is usable as a reference without checkout. Key reusable pieces are standalone inference-only evaluation, per-episode env config injection, manifest/result/AI-summary artifacts, per-step JSONL logs, charge/slack/recoverability metrics, reward attribution windows, and optional parallel task scheduling.
2026-04-25 - T0: Local `win_YJY` benchmark must intentionally override LTSPPO's broader multi-round defaults and stay fixed at `max_step=1000`, `battery_max=150`, `robot_count=4`, `charger_count=3`, maps `[4,7]`, `10` episodes per map (`2*10`).
2026-04-25 - T0: Current local analysis helpers already support checkpoint/per-map/fail-rate summaries through `code/agent_ppo/utils/archive_analysis.py`; T2 can build a lightweight holdout runner/report around active `code/` instead of copying LTSPPO algorithm/network changes.
2026-04-25 - T1: Baseline capture anchored at HEAD `a34e9aa` with `origin/win_YJY`; `python train/resume_best.py latest` matched `code/model.ckpt-resume.meta.json` (`clean_score=704.0`, `episode_cnt=74`, `saved_at=2026-04-25 04:53:38`, `trigger=time`) and model hash/mtime inventory was recorded for mutation checks.
2026-04-25 - T7: Documentation coverage updated to reflect actual train/context contents. Pre-existing raw-ish episode text files were found in train/context (episodes_raw.txt, episodes_raw2.txt, episodes_raw3.txt) and flagged for review. No new bulky artifacts were introduced; train/context remains lightweight aside from these pre-existing inputs.
2026-04-25 - T2: A safe local holdout contract can stay outside PPO/runtime code by hard-locking maps `[4,7]`, re-reading training maps from `train_env_conf.toml`, snapshotting `latest_model.pkl` / resume artifacts before and after dry-run, and emitting planned per-episode/schema artifacts for T3 to reuse.
2026-04-25 - T2: The analyzer contract should always materialize `combined` plus per-map `4` and `7` summaries even when no real episodes exist yet; `status=NO_EPISODES` keeps closed-loop consumers from crashing on dry-run outputs.
2026-04-25 - T3: Non-dry-run baseline attempt still produces a useful failure artifact/analyzer report: fixed maps `[4,7]`, planned episode count 20, `NO_EPISODES`, and model mutation guard stayed clean (`mutation_detected=false`, no drifted paths).
2026-04-25 - T8: Holdout analyzer can stay analyzer-only while becoming future-ready by emitting fixed zero-count failure buckets for `NO_EPISODES`, layering optional replay/archive evidence sources on top of benchmark JSON, and always collapsing to a single `next_step` decision (`NEED_MORE_DATA` when evidence is insufficient).
2026-04-25 - T8: Replay-backed diagnosis can be added conservatively with heuristics over existing fields from `train_workflow.py` (`death_replay_path`, `charger_arrived_count`, `charger_first_arrival_step`, `invalid_move_rate`, `trajectory[].charger_slack`, `trajectory[].nearest_npc_dist`) without changing PPO or runner semantics.
2026-04-25 - T8-fix: Analyzer metrics/classification must use shared reason predicates instead of exact string equality because holdout/runtime artifacts may emit aliases such as `battery_fail`, `battery_death`, `out_of_battery`, and `collision_fail`.
2026-04-25 - T4: Best-checkpoint promotion in `train_workflow.py` now requires `fail_reason == "completed"`, while failed episodes still flow through summary, replay, and failure telemetry.
2026-04-25 - T5: Coverage battery gating can be tightened with a local-only buffer near `_select_coverage_target()` without touching global return thresholds or charger A* behavior; keep the scope explicit in evidence.
2026-04-25 - T6: Closed-loop orchestration can remain safe as a planner/decision CLI: dry-run prints Docker/benchmark/analyzer/revert templates only, while decision mode consumes existing analysis JSON and gates ACCEPT/REJECT/CONTINUE without launching runtime paths.
2026-04-25 - T6: The hard loop target is strictly holdout combined avg `>900`; non-winning rounds only trigger the escalation ladder after 3 consecutive misses and must not become a fixed max-round stop condition.
2026-04-25 - T6-fix: Acceptance must be a two-part gate: `(score improvement >=20 OR combined avg > target)` plus stability `(completed_drop <=0.05 AND clean_per_step_drop <=2%)`; a >900 candidate with bad stability should reject/continue, not auto-accept.
2026-04-25 - T10: Reward/refactor/network escalation must be treated as an evidence gate when T3 has no real episodes; `NO_EPISODES` plus T8 `NEED_MORE_DATA` means the correct output is documentation/evidence, not PPO behavior changes.
2026-04-25 - T9: Final checkpoint selection must remain NOT_READY until real `[4,7]` holdout evidence exists with combined avg `>900`; packaging/signing remains a template only.
